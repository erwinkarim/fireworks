%caption
  %strong Historical User Listings on #{feature.created_at.localtime.strftime('%A, %d-%b-%Y @%H:%M:%S') }
%thead
  %tr
    %th User Info    
%tbody
  - if users.empty? then
    %tr
      %td No Users Detected
  - else
    - users.each do |user|
      %tr
        %td
          = link_to(user[:username] , user_path(user[:user_id])) + '@' + user[:machinename]
