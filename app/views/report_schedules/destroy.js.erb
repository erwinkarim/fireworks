console.log('delete requested');

$(document).ready( function(){
  //add alert banner
  var alertmsg = $('<div/>', { class:'alert fade in', text:'Report Deleted' });
  alertmsg.append( 
    $('<button/>', { class:'close', 'data-dismiss':'alert'}).append(
      $('<i/>', { class:'fa fa-times' })
    )
  );
  $('.yield_div').prepend(alertmsg);

  $('.accordion-group[data-id=' + <%= @rs.id %> + ']').hide('slow');
});
