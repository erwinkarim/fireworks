.row
	.span12
		.page-header
			%h1 
				- if ads_user_signed_in? then
					= watch_icon( {:user => current_ads_user, :model_type => 'ReportSchedule', :model_id => @schedule.id })
				#{@schedule.title}
			%ul.breadcrumb
				%li
					%a{ :href => report_schedules_path } Reports
				%li.active #{@schedule.title }
.row
	.span12
		.info{ :data => { :'schedule-id' => @schedule.id } }
			= fa_icon('cog spin 4x', { :text => 'Loading...' })
