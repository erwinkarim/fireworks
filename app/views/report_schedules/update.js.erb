console.log('report updated');

$(document).ready( function(){

  //add alert banner
  var alertmsg = $('<div/>', { class:'alert fade in', text:'Report Updated' });
  alertmsg.append( 
    $('<button/>', { class:'close', 'data-dismiss':'alert'}).append(
      $('<i/>', { class:'fa fa-times' })
    )
  );
  $('.yield_div').prepend(alertmsg);

});
