- unless @users.empty? || @users.nil? 
	- @users.each do |user|
		.accordion-group{ :data => { :init => 'false' , :id => user.id } }
			.accordion-heading
				%a.accordion-toggle{ :data => { :toggle => 'collapse', :parent => '#user-listings' }, :href => '#user' + user.id.to_s }
					= user.name
			.accordion-body.collapse{ :id => 'user' + user.id.to_s }
				.accordion-inner
					.links
						- if ads_user_signed_in? then
							= watch_icon({ :user => current_ads_user, :model_type => 'User', :model_id => user.id, :class => 'btn', :text => true })
						= link_to fa_icon('link', { :text => 'Permalink' }), user_path(user), { :class => 'btn' }
					%br
					= render :template => "users/show.template", :locals =>  { :@user => user }
